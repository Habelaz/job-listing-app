'use client';

import { signOut, useSession } from 'next-auth/react';
import Link from 'next/link';

const Nav = () => {

  return (
    <div className='nav'>
      logout
    </div>
  );
};

export default Nav;
